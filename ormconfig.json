{
    "type": "sqlite",
    "database": "database/typeorm-relations.db",
    "entities": ["dist/**/**/*.entity{.ts,.js}"],
    "synchronize": false, 
    "migrations": [
        "dist/database/migrations/*.js"
    ],
    "cli": {
        "migrationsDir": "database/migrations"
    }
}